{
  "name": "big-ten-sports-app",
  "version": "1.0.0",
  "description": "Big Ten sports scores, standings, and rankings — mobile-first pop-up app",
  "main": "server.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build:client": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
