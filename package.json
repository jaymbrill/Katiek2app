{
  "name": "r2r2r-training",
  "version": "1.0.0",
  "description": "Strava-integrated R2R2R training app with daily SMS workout recommendations",
  "main": "server.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build:client": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.111.0",
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
